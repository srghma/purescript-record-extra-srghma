module Test.Tests.MapRecordSpec where

import Prelude

import Test.Spec (Spec, describe, it)

spec :: Spec Unit
spec =
  describe "MapRecordTests" do
    it "discovers the specs again in this project" $ pure unit
